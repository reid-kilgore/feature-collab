# Pressure Tests

Use adversarial scenarios to expose where prompts rationalize, skip gates, or claim unverifiable progress.

Minimum first targets:

- orchestrator
- code-architect
- test-runner
- scope-guardian
