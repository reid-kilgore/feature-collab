# Roles

Portable role contracts define responsibilities, boundaries, evidence standards, and common failure modes.

Each runtime adapter can render these into runtime-native prompt files or instruction bundles.
