# RETIRED — do not treat this repository as a source of truth

This repository is kept only for its history. Nothing here is published or loaded by any harness.
The plugin it defines is registered as a marketplace but is not installed, so its commands, agents,
and hooks serve nothing.

The live source of truth is the canonical bundle:
`/Users/reid/dev/meta-agent-repo/canonical-bundle/content/`

- skills — `content/skills/`
- roster agents — `content/agents/`
- Claude instructions — `content/instructions/claude/CLAUDE.md`
- Codex instructions — `content/instructions/codex/AGENTS.md`

Copies here are older than the canonical ones and contradict them in places. Following a rule from
this repository will make you do the wrong thing.

**If you arrived here by accident — a search hit, a stale path, a link in an old document — you
MUST tell the user you found it, name the file that sent you here, and ask whether that pointer
should be fixed.** Do not quietly read on, and do not act on anything you find here. Silently using
a retired source is the failure this notice exists to prevent.

Editing anything here is almost certainly a mistake. Make the change in the canonical bundle.
