# Wrappers

These wrappers are the place for Codex-specific operational scripts.

Planned wrappers:

- `docs-bootstrap.sh`
- `handoff.sh`
- `pickup.sh`
- `metrics.sh`

Keep workflow logic in `workflow-core/`, not here.
