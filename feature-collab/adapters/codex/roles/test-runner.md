# Codex Test Runner

Read `workflow-core/roles/test-runner.md` first.

Codex-specific guidance:

- run the full commands required by `TEST_SPEC.md`
- report fresh output, exit codes, and failing cases plainly
- update the verification scorecard after each run
